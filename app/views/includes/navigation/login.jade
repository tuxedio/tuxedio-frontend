li.dropdown(ng-hide="signedin")
  a.dropdown-toggle(data-toggle='dropdown')
    | Log in
  ul.dropdown-menu(role='menu')

    //- Right Login Section
    li.navbar-form
      form(ng-submit="submit()" role='form')
        .h4 Log into Tuxedio
        .login-message(ng-if="message"){{message}}
        .form-group
          input.form-control(ng-model="login.user.email" type='email', id='inputEmail', name="user", placeholder='Email')
        .form-group
          input.form-control(ng-model="login.user.password" type='password', id='inputPassword', name="pass", placeholder='Password')
        .form-group
          .checkbox
            label
              input(type='checkbox')
              |  Remember me
        .form-group
          .col-sm-offset-4.col-sm-8
            button.btn.btn-success(type='submit', value="Login") Sign in

    //- Right Login Section
    li.divider
    li
      h4 Don't have an account yet?
      p Lorem ipsum dolor sit amet, consectetur adipiscing elit. Aenean euismod bibendum laoreet. Proin gravida dolor sit amet lacus accumsan et viverra justo commodo.
      button.btn.btn-default(href='#') Sign up for an account!
